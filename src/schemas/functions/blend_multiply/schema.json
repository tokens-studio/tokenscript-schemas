{
  "name": "Blend Multiply",
  "type": "function",
  "description": "Multiplies two colors channel by channel. Result is always darker or equal. Standard Photoshop/CSS blend mode. Formula: C = A × B (per channel). Useful for shadows, darkening, and color filtering.",
  "keyword": "blend_multiply",
  "input": {
    "type": "object",
    "properties": {
      "base": {
        "type": "color",
        "description": "Base/bottom color"
      },
      "blend": {
        "type": "color",
        "description": "Blend/top color"
      }
    }
  },
  "script": {
    "type": "/api/v1/core/tokenscript/0/",
    "script": "./blend-multiply.tokenscript"
  },
  "requirements": ["/api/v1/core/srgb-color/0/"]
}

